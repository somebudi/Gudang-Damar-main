import 'package:flutter/material.dart';

/// Palette warna Gudang Damar — diselaraskan dengan tema web.
class AppColors {
  static const primary       = Color(0xFF66ACE6);
  static const primaryDark   = Color(0xFF4F9BD1);
  static const buttonDark    = Color(0xFF1A1A1A);
  static const textPrimary   = Color(0xFF111111);
  static const textSecondary = Color(0xFF64748B);
  static const inputBorder   = Color(0xFFE2E8F0);
  static const inputBg       = Color.fromRGBO(255, 255, 255, 0.5);
  static const cardGlass     = Color.fromRGBO(255, 255, 255, 0.25);
  static const errorRed      = Color(0xFFDC2626);
}